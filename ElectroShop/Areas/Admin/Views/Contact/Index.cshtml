@model IEnumerable<ElectroShop.Models.MContact>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@Html.Partial("_MessageAlert")
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3" id="sidebar">
            <ul class="nav nav-list bs-docs-sidenav nav-collapse collapse">
               
                <li class="active">
                    <a href="@Url.Action("Index","Contact")"><i class="icon-chevron-right"></i> Danh sách</a>

                </li>
                <li class="">

                    <a href="@Url.Action("Trash","Contact")"><i class="icon-chevron-right"></i> Thùng rác(@ViewBag.count_trash)</a>
                </li>
            </ul>
        </div>

        <div class="span9" id="content">
            <div class="row-fluid">
                <div class="navbar">
                    <div class="navbar-inner">
                        <ul class="breadcrumb">
                            <i class="icon-chevron-left hide-sidebar"><a href='#' title="Hide Sidebar" rel='tooltip'>&nbsp;</a></i>
                            <i class="icon-chevron-right show-sidebar" style="display:none;"><a href='#' title="Show Sidebar" rel='tooltip'>&nbsp;</a></i>
                            <li>
                                <a href="#">Liên hệ</a>
                            </li>


                        </ul>
                    </div>
                </div>
                <!-- block -->
                <div class="block">

                    <div class="block-content collapse in">
                        <div class="span12">
                            <div class="table-toolbar">
                                <div class="btn-group">

                                </div>

                            </div>

                            <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered dataTable" id="example2" aria-describedby="example2_info">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Tiêu đề liên hệ</th>
                                        <th>Email</th>
                                        <th>Điện thoại</th>
                                        <th >Trạng thái</th>
                                        <th >Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{var stt = 1;}
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td class="text-center">@Html.DisplayFor(modelItem => item.Id)</td>
                                            <td>@Html.DisplayFor(modelItem => item.Title)</td>
                                            <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                            <td>0@(Html.DisplayFor(modelItem => item.Phone))</td>

                                            <td class="text-center">

                                                @if (item.Flag == 1)
                                                {
                                                    <span class="badge badge-secondary">
                                                        Đã trả lời
                                                    </span> }
                                                else
                                                {
                                                    <span class="badge badge-success">
                                                        Chưa trả lời
                                                    </span>}

                                            </td>
                                            <td class="text-center">
                                                @if (item.Flag == 1)
                                                {
                                                    <a class="btn btn-secondary btn-sm" href="@Url.Action("Reply", "Contact", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Xem">
                                                        Đã trả lời
                                                    </a> }
                                                else
                                                {
                                                    <a class="btn btn-info btn-sm" href="@Url.Action("Reply", "Contact", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Trả lời @Html.DisplayFor(modelItem => item.FullName)">
                                                        <i class="fas fa-edit"></i> Trả lời
                                                    </a>}
                                                <button type="button" class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="top" title="Xóa liên hệ">
                                                    <a href="@Url.Action("DelTrash", "Contact", new { id = item.Id })" class="text-white"><i class="icon-trash"></i></a>
                                                </button>
                                            </td>
                                        </tr>
                                        stt++;
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <!-- /block -->
            </div>
        </div>
    </div>
    <hr>

</div>
