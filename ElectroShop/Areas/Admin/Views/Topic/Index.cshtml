@model IEnumerable<ElectroShop.Models.MTopic>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@Html.Partial("_MessageAlert")
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3" id="sidebar">
            <ul class="nav nav-list bs-docs-sidenav nav-collapse collapse">
                <li class="">
                    <a href="@Url.Action("Create","Topic")"><i class="icon-chevron-right"></i> Thêm mới</a>

                </li>
                <li class="active">
                    <a href="@Url.Action("Index","Topic")"><i class="icon-chevron-right"></i> Danh sách</a>

                </li>
                <li class="">

                    <a href="@Url.Action("Trash","Topic")"><i class="icon-chevron-right"></i> Thùng rác(@ViewBag.demrac)</a>
                </li>
            </ul>
        </div>

        <div class="span9" id="content">
            <div class="row-fluid">
                <div class="navbar">
                    <div class="navbar-inner">
                        <ul class="breadcrumb">
                            <i class="icon-chevron-left hide-sidebar"><a href='#' title="Hide Sidebar" rel='tooltip'>&nbsp;</a></i>
                            <i class="icon-chevron-right show-sidebar" style="display:none;"><a href='#' title="Show Sidebar" rel='tooltip'>&nbsp;</a></i>
                            <li>
                                <a href="#">Chủ đề bài viết</a>
                            </li>


                        </ul>
                    </div>
                </div>
                <!-- block -->
                <div class="block">

                    <div class="block-content collapse in">
                        <div class="span12">
                            <div class="table-toolbar">
                                <div class="btn-group">

                                </div>

                            </div>

                            <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="example2">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Tên chủ đề</th>
                                        <th>Slug</th>
                                        <th>Ngày tạo</th>
                                        <th>Trạng thái</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                <tr>
                                    <td class="text-center">@Html.DisplayFor(modelItem => item.Id)</td>
                                    <td><a href="~/Admin/Topic/Details/@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>  </td>
                                    <td><a href="~/@item.Slug">@Html.DisplayFor(modelItem => item.Slug)</a> </td>
                                    <td>  @Html.DisplayFor(modelItem => item.Created_at)</td>
                                    <td>
                                        <label class="switch">
                                            @if (item.Status == 1)
                                            {<input class="changeStatus" data-id="@item.Id" data-controller="Topic" type="checkbox" checked>}
                                            else
                                            {<input class="changeStatus" data-id="@item.Id" data-controller="Topic" type="checkbox">}
                                            <span class="slider round"></span>
                                        </label>
                                    </td>
                                    <td class="text-center">
                                        
                                        <button type="button" class="btn btn-primary btn-sm" data-toggle="tooltip" data-placement="top" title="Chỉnh sửa">
                                            <a href="@Url.Action("Edit", "Topic", new { id = item.Id })" class="text-white"><i class="icon-edit"></i></a>
                                        </button>
                                        <button type="button" class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="top" title="Xóa">
                                            <a href="@Url.Action("DelTrash", "Topic", new { id = item.Id })" class="text-white"><i class="icon-trash"></i></a>
                                        </button>
                                    </td>
                                </tr>}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <!-- /block -->
            </div>
        </div>
    </div>
    <hr>

</div>
